<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/main_background" >

    <TextView
        android:id="@+id/activity_qa_emptyview_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:textColor="#000" />

    <android.support.v4.widget.SwipeRefreshLayout
        android:id="@+id/swype"
        android:layout_width="match_parent"
        android:layout_height="match_parent" >

        <ListView
            android:id="@+id/activity_qa_listView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_above="@+id/activity_qa_rl"
            android:layout_marginLeft="3dp"
            android:layout_marginRight="3dp"
            android:cacheColorHint="@android:color/transparent"
            android:dividerHeight="1dp"
            android:drawSelectorOnTop="true"
            android:listSelector="@drawable/list_selector" >
        </ListView>
    </android.support.v4.widget.SwipeRefreshLayout>

    <RelativeLayout
        android:id="@+id/activity_qa_rl"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="@drawable/preference_item"
        android:paddingBottom="5dp"
        android:paddingTop="5dp" >

        <com.ruptech.chinatalk.widget.EditTextWithDel
            android:id="@+id/activity_qa_message"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_toLeftOf="@+id/activity_qa_btn_send"
            android:ems="10"
            android:hint="@string/please_input_your_question"
            android:inputType="textMultiLine"
            android:maxHeight="150dp"
            android:minHeight="32dp"
            android:textAppearance="?android:attr/textAppearanceSmall" >

            <requestFocus />
        </com.ruptech.chinatalk.widget.EditTextWithDel>

        <Button
            android:id="@+id/activity_qa_btn_send"
            style="@style/white_button"
            android:layout_width="wrap_content"
            android:layout_height="32dp"
            android:layout_alignParentRight="true"
            android:layout_centerVertical="true"
            android:text="@string/send"
            android:textAppearance="?android:attr/textAppearanceSmall"
            android:textSize="15sp" />
    </RelativeLayout>

</RelativeLayout>